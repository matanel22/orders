const sumForTarget = (array, target) => {
  let index1 = 0;
  let index2 = 0;
  for (let i = 0; i < array.length; i++) {
    let obj = { key: target - array[i] };
    obj.key;
  }
};
const array = [1, 2, 2, 4];
let target = 5;
console.log(sumForTarget(array, target));
